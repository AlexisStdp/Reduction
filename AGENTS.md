# Project Instructions

- Before running any Python command, including `python`, `py`, `.venv\Scripts\python.exe`, `pytest`, or scripts that invoke Python, ask the user for confirmation first.

- Do not modify the reduction.py file. If you need to make changes, ask the user for permission first.